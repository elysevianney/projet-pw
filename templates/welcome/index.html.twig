{% extends 'base.html.twig' %}

{% block title %}Jobify{% endblock %}
    {% block menuProfil %}
{% if(app.user and is_granted('ROLE_DEV'))  %}
    {{ include('menu_dev.html.twig') }}                                                                  
{% elseif (app.user and is_granted('ROLE_COMPANY')) %}
    {{ include('menu_company.html.twig') }}  
{% endif %}
{% endblock menuProfil %}
{% block body %}
                <div class="row mb-3">
                    <img src="{{ asset('asset/images/card/pic6.jpg')}}" class="img-fluid" alt="">
                </div>
				<div class="row">
                    {% if(app.user and is_granted('ROLE_DEV'))  %}
                        <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20">Offres populaires</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for post in bestPosts %}
                                        <a href="{{ path('app_post_show', {'id': post.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ post.user.company.logo) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ post.name  }} </h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Ville : {{ post.city }}</span>
                                                            <span class="fs-14 text-nowrap">{{ post.experience }} ans d'expérience requises</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas d'offres trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>

                    <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20"> Nouvelles publication de Poste</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for post in lastThreePosts  %}
                                        <a href="{{ path('app_post_show', {'id': post.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ post.user.company.logo) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ post.name  }} </h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Ville : {{ post.city }}</span>
                                                            <span class="fs-14 text-nowrap">{{ post.experience }} ans d'expérience requises</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas de postes trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>                                                               
                    {% elseif (app.user and is_granted('ROLE_COMPANY')) %}
                        <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20">Développeurs les plus consultés</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for dev in bestDevs %}
                                        <a href="{{ path('app_dev_show', {'id': dev.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ dev.avatar) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ dev.lastname  }} {{ dev.firstname  }}</h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Note : {{ dev.rating }}</span>
                                                            <span class="fs-14 text-nowrap">{{ dev.experience }} ans d'expérience</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas d'offres trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>

                    <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20"> Nouveaux développeurs</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for dev in lastThreeDevs %}
                                        <a href="{{ path('app_dev_show', {'id': dev.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ dev.avatar) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ dev.lastname  }} {{ dev.firstname  }}</h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Note : {{ dev.rating }}</span>
                                                            <span class="fs-14 text-nowrap">{{ dev.experience }} ans d'expérience</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas de développeurs trouvés trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div> 
                </div>     
                    {% else %}

                <div class="row mb-3">
                    <h1 class="text-primary text-center">Trouvez facilement votre futur emploi!</h1>
                </div>
                <div class="row">
                    
                        <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20">Offres populaires</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for post in bestPosts %}
                                        <a href="{{ path('app_post_show', {'id': post.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ post.user.company.logo) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ post.name  }} </h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Ville : {{ post.city }}</span>
                                                            <span class="fs-14 text-nowrap">{{ post.experience }} ans d'expérience requises</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas d'offres trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>

                    <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20"> Nouvelles publication de Poste</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for post in lastThreePosts  %}
                                        <a href="{{ path('app_post_show', {'id': post.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ post.user.company.logo) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ post.name  }} </h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Ville : {{ post.city }}</span>
                                                            <span class="fs-14 text-nowrap">{{ post.experience }} ans d'expérience requises</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas de développeurs trouvés trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>                                                               
                    
                        <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20">Développeurs les plus consultés</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for dev in bestDevs %}
                                        <a href="{{ path('app_dev_show', {'id': dev.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ dev.avatar) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ dev.lastname  }} {{ dev.firstname  }}</h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Note : {{ dev.rating }}</span>
                                                            <span class="fs-14 text-nowrap">{{ dev.experience }} ans d'expérience</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas d'offres trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>

                    <div class="col-xl-6 col-xxl-6">
						<div class="row">
							<div class="col-xl-6 col-xxl-12">
								<div class="card latest-sales-bx">
									<div class="card-header border-0 mb-0">
										<h4 class="fs-20"> Nouveaux développeurs</h4>
										
									</div>
									<div class="card-body pb-0 dz-scroll loadmore-content pt-0" id="LatestSalesContent">
										{% for dev in lastThreeDevs %}
                                        <a href="{{ path('app_dev_show', {'id': dev.id}) }}">
                                            <div class=" pb-0 dz-scroll loadmore-content pt-0" >
                                                <div class="media pb-3 border-bottom mb-3 align-items-center">
                                                    <div class="media-image me-2">
                                                        <img src="{{ asset('uploads/avatars/' ~ dev.avatar) }}" alt="">
                                                    </div>
                                                    <div class="media-body">
                                                        <h6 class="fs-16 mb-0">{{ dev.lastname  }} {{ dev.firstname  }}</h6>
                                                        <div class="d-flex">
                                                            <span class="fs-14 me-auto text-primary">Note : {{ dev.rating }}</span>
                                                            <span class="fs-14 text-nowrap">{{ dev.experience }} ans d'expérience</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                        {% else %}
                                            <h6 class="m-3 alert alert-danger">Pas de développeurs trouvés trouvés</h6>
                                        {% endfor %}
									</div>
									
								</div>
							</div>
							
						</div>	
					</div>  

                    {% endif %}
					
				</div>	
{% endblock body %}
            