<form method="post" action="{{ path('app_dev_delete', {'id': dev.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dev.id) }}">
    <button class="btn btn-danger">Supprimer votre compte</button>
</form>
